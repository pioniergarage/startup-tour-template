name: Deploy Admin site to Firebase Hosting on merge
'on':
  push:
    branches:
      - master
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    - name: debug
      run: echo ${{ secrets.FTP_KEY }}
    - name: 📂 Sync files
      uses: sebastianpopp/ftp-action@releases/v2
      with:
        host: https://ssh.strato.de
        user: ftp_startup_touren@pioniergarage.de
        password: ${{ secrets.FTP_KEY }}
        localDir: ./WebSite/